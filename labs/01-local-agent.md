# Lab 1: Build the local agent path

```bash
make data
make demo
```

Inspect `reports/demo_run.md` and `data/warehouse/agent_runs.jsonl`. Identify which actions were allowed, blocked, or staged for approval.

Challenge: add a new ticket category and update `classify_intent`.
